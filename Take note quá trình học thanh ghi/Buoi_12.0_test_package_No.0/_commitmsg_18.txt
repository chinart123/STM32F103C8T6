§18 docs(kb+take-note): KI-IAR-0006 (.ewp vs .ewd) + educational deep-dive with real snippets and figure

Educational pair born from the user's real question during Buoi_12.0 ("IAR
touched test.ewd, we leave it uncommitted — what IS that?"):

- docs/detected-issues/.../toolchain-iar/KI-IAR-0006__ewd-debugger-file-churn-vs-ewp/
  New KB entry (scope: toolchain, phase: run — the user correctly noted this
  belongs to the run side, not compile/build). IAR splits one project into a
  BUILD notebook (test.ewp) and a DEBUGGER notebook (test.ewd, one C-SPY block
  per configuration); creating Debug_Vendor in the GUI made IAR silently write
  +1481 lines into .ewd. error.log carries the verbatim git evidence
  (status + diff --stat + the head of the auto-written <configuration> block);
  refs point at IAR's IDE Project Management guide file-types table. Policy
  confirmed: commit .ewp, never .ewd (skip-worktree offered for a clean panel).
  KB catalogue README gains the row.

- Take note .../Buoi_12.0_test_package_No.0/Giai_thich_ewp_vs_ewd.md (+ .docx
  twin with clickable TOC + List of Figures) — the plain-language deep-dive
  the user asked for, with REAL snippets
  from this very project (CCIncludePath2 block from .ewp vs OCDynDriverList/
  FlashLoadersV3 from .ewd), the git evidence, the two options for the nagging
  "M" mark, and the transferable lesson (every IDE splits shared-asset config
  from per-machine config: Keil .uvprojx/.uvoptx, VS Code settings, CMake).
  fig_12_0_ewp_ewd.png: TikZ anatomy of the file trio with git verdict badges
  (figure-skill self-inspect, passed first round).

§-section convention: this file `_commitmsg_18.txt` IS this commit's message
(`git commit -F`). Per retention rule B1, `_commitmsg_17.txt` (same folder) is
removed in this commit; the notes branch keeps all of its own message files.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01XREkaAL5M7JJfbY1oBvu31
