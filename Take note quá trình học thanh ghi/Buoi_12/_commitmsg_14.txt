§14 docs(take-note): add Buoi_12 roadmap — two-track DMA plan (Roadmap_12.md + .docx companion)

Adds `Take note quá trình học thanh ghi/Buoi_12/`:

- Roadmap_12.md  — the pre-lesson plan for the DMA chapter, restructured for
  readability: every dense block is now a pipe TABLE (the three
  "how-does-the-CPU-know-it's-done" mechanisms; the 5-level chapter ladder with
  its vendor-track column; the hard DMA1 channel map; per-register value tables
  for lesson 12.2; the five IAR include paths with a shared $REPO$ prefix and a
  "why needed" column backed by the measured include chain of hal_def.h; defined
  symbols; Add-Files rules per track; the two-track acceptance procedure).
  Content covers, as requested: what the own-API stage looks like (raw register
  skeletons 12.1/12.2, files to create, planted bugs, C-SPY capture plan), what
  the vendor-API stage looks like (LL-not-HAL rationale, the warehouse->Draft
  git mv ritual bound to the birth of the hand-written dma.h, line-for-line LL
  skeleton), and the full IAR setup/config (separate "Debug_Vendor" build
  configuration so the default Debug config keeps zero vendor paths — the
  NO HAL/NO LL boundary enforced by structure).
- fig_12_two_track.png / fig_12_datapath.png — two TikZ figures produced by the
  figure skill per FIGURE-STANDARD (colorblind-safe palette, mandatory legend,
  orthogonal routing) and passed through the render->self-inspect loop (one fix
  round: DIFF-box line wrap; label air-gap before the TIM1 block). Embedded in
  both the .md (readable on GitHub) and the .docx (numbered "Hình N" captions).
- Roadmap_12.docx — hand-off companion generated from the .md by the frozen v1
  md2docx bridge (docx-as-export-bridge, per platform v2 section 1), now with a
  clickable Table of Contents and List of Figures ([[TOC]]/[[LOF]] injected at
  build time so the repo .md stays marker-free); regenerate with md2docx.py
  whenever the .md changes.

The working sketch stays untouched in scratch/ (local, git-ignored) for
cross-reference. This commit replaces a discarded draft of §14 (never pushed).

§-section convention: this file `_commitmsg_14.txt` IS this commit's message
(`git commit -F`). Per retention rule B1, `docs/_commitmsg_13.txt` is removed
in this commit; the notes branch keeps all of its own message files.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01XREkaAL5M7JJfbY1oBvu31
