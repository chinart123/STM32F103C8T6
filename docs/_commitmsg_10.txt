§10 docs(platform): add Definition-of-Done checklist + clarify "legacy" boundary

Two small patches to the platform v2 blueprint, both promoting session knowledge
into multi-model law (invariant B5: one rule, one home):

- New section 3.1 "Definition of Done": the 8-item acceptance checklist every
  completed request must satisfy — detected-issues entry (conditional), public
  docs, Buoi_N hand-note snapshot (conditional, user-reviewed before commit),
  paired commit-message files, generated session report + INDEX, firmware-logic
  bugs routed to the local bug log, figure-skill discipline for report figures,
  and the B2 pair check before the two pushes. Fractional § numbers (e.g. §9.5)
  are recorded as the sanctioned pattern for backfill commits between stages.

- Section 5.2 clarification: "legacy" has two anchors that must not be conflated —
  the old docx report artifacts are §6/§7, while the unpaired main<->note commits
  (per invariant B2) span §5–§7. The content inside §1–§5 (code, hand-notes, KB)
  remains inheritable learning material; legacy only means no retroactive fixes.

Rationale: a cross-session review by a second model had to re-derive the §5 vs
§6/§7 boundary from context — one explicit line removes that ambiguity for every
future reader. The checklist previously lived only in one assistant's private
memory; the blueprint is its single home now, memory holds only a pointer.

§-section convention: this file `_commitmsg_10.txt` IS this commit's message
(`git commit -F`). Per the retention rule (invariant B1), main keeps only the
newest `_commitmsg` at HEAD — `Take note quá trình học thanh ghi/Buoi_11/
_commitmsg_9.5.txt` is removed in this commit; the notes branch keeps all of
its own.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01XREkaAL5M7JJfbY1oBvu31
