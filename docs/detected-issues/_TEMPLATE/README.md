# KI-XXX-NNNN — <tiêu đề ngắn gọn>

**Triệu chứng:** <mô tả điều quan sát được: build/link/runtime báo gì, khi nào>.

## Chuỗi nguyên nhân
1. <bước 1>
2. <bước 2>
3. <… tới khi ra thông báo lỗi cuối cùng, giải thích "vì sao lại thành lỗi này">

## Vị trí code (hiện tại)
`<path/tới/file>` (dòng <n>):
```c
<đoạn code dính lỗi — chỉ cần vị trí HIỆN TẠI>
```

## Cách sửa
<đã/đang sửa thế nào; nếu chưa có thì ghi "chưa có (bug mở)">.

## Bài học / phòng ngừa
<rút ra gì để lần sau không dính>.

## Trạng thái
`fixed` | `open` | `mitigated`. Nếu `open`: <hướng truy khi tái hiện được>.
